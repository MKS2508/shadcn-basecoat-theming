---
export interface Props {
  value?: number;
  max?: number;
  class?: string;
}

const { value = 0, max = 100, class: className = "" } = Astro.props;
---

<progress 
  class={`progress ${className}`.trim()}
  value={value}
  max={max}
>
  {value}%
</progress>